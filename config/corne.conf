CONFIG_BT=y

# Power management: enable deep sleep for better wireless battery life.
# Idle (lower BLE poll rate) after 30s is the default; deep sleep after 30 min.
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

